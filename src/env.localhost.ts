import { EnvironmentConfig } from "./index";

const EnvLocalhost : EnvironmentConfig = {
  localhost: [
    { value: "wss://localhost:7000", text: "LOCAL ER A" },
    { value: "wss://localhost:7001", text: "LOCAL ER B" },
  ]
};

export default EnvLocalhost;